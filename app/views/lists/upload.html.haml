%h1 User upload results
%ul
  - if @uploaded_users.present?
    - @uploaded_users.each do |user|
      %li== #{user.fname} #{user.lname} (#{user.email}) added successfully to the list "#{@list.name}"
  - else
    %li <b>None</b>